module "papi_redis" {
  source              = "../../../../modules/databases/redis-elasticcache/"
  security_groups     = ["${data.terraform_remote_state.vpc.redis_sg_id}"]
  env                 = "${var.env}"
  name                = "${var.name}"
  desired_clusters    = "${var.desired_clusters}"
  subnet_ids          = "${data.terraform_remote_state.vpc.public_subnets}"
  cache_identifier    = "${var.cache_identifier}"
  maintenance_window  = "${var.maintenance_window}"
  port                = "${var.port}"
  zone_id_public      = "${var.zone_id_public}"
  zone_id_private     = "${var.zone_id_private}"
  route53_record      = "${var.route53_record}"
  common_topic_arn    = "${var.common_topic_arn}"
  comparison_operator = "${var.comparison_operator}"
  evaluation_periods  = "${var.evaluation_periods}"
  cpu_metric_name     = "${var.cpu_metric_name}"
  namespace           = "${var.namespace}"
  period              = "${var.period}"
  statistic           = "${var.statistic}"
  threshold           = "${var.threshold}"
  node_type           = "${var.node_type}"
  engine_version      = "${var.engine_version}"
  snapshot_retention_limit = "${var.snapshot_retention_limit}"
  automatic_failover_enabled = "${var.automatic_failover_enabled}"
  #  snapshot_window            = "${var.snapshot_window }"
  comparison_operator_cachmem = "${var.comparison_operator_cachmem}"
  memory_metric_name          = "${var.memory_metric_name}"
  redis_memory_threshold      = "${var.redis_memory_threshold}"
  replicationlag_metric_name  = "${var.replicationlag_metric_name}"
  replicationlag_threshold    = "${var.replicationlag_threshold}"
  swapmem_metric_name         = "${var.swapmem_metric_name}"
  swapmem_threshold           = "${var.swapmem_threshold}"
  currconnection_metric_name  = "${var.currconnection_metric_name}"
  currconnection_threshold    = "${var.currconnection_threshold}"
}
